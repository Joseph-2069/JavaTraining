package MultiThreading;


//Synchronisation issue occurs when the two threads are started 
//lock aka monitor
//only one thread can acquire a lock at once
//wait() allows the thread to wait inside the synchronized method
//when invoked the current thread is blocked and gives object lock
//waits to be notified by another thread of a change in theis object
//
//
//NOTIFY,NOTIFYALL
//Notify(): wakes uo a single thread that is waiting
//THis method should only be called by thread that is the owner of the obejcts monitor
//NotifyAll() : wakes up all threads that aew waiting on this object


//THREAD GROUP
//* sone progeams contain a number og threads
//it would

public class Synchronisation {

}
